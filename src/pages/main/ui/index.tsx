import { Container } from '@mui/material';
import { UsersList } from '@/widgets/users';

const MainPage = () => {
  return (
    <Container>
      <UsersList />
    </Container>
  );
};

export default MainPage;
